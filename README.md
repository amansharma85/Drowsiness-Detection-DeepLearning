## Drowsiness Detection using Deep Learning

This project detects driver drowsiness in real time using a CNN-based eye state classifier
and alerts the user with an audio alarm.

### How to Run
```bash
pip install -r requirements.txt
python detect_drowsiness.py
